export const environment = {
  production: true,
  // Laravel API base URL for production
  // TODO: Update this to your actual production API URL before deployment
  apiUrl: 'https://your-production-api.com/api'
};
